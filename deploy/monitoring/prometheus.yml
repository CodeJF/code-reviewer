global:
  scrape_interval: 15s
  evaluation_interval: 30s

rule_files:
  - /etc/prometheus/rules.yml

scrape_configs:
  - job_name: iot-ops-api
    metrics_path: /internal/metrics
    static_configs:
      - targets: ["api:8000"]
